"""Oliver — the R/W Book Club's Discord agent.

A discord.py bot that answers questions in the #ask-oliver channel by sending
them to Claude (anthropic SDK) with the knowledge corpus as context. Run from
the repo root as `python -m agent.bot`.
"""
