# Oliver reuses the corpus package (shared data paths + deps), so it needs the
# corpus deps too.
-r ../corpus/requirements.txt

discord.py>=2.7.1
anthropic>=0.117.0
markdown>=3.10.2

# Member/admin web app (agent/webapp/): aiohttp server + Jinja2 templates.
# aiohttp also ships with discord.py, but the webapp imports both directly.
aiohttp>=3.12
jinja2>=3.1
