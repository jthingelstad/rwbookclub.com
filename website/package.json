{
  "name": "rwbookclub-com",
  "version": "0.1.0",
  "private": true,
  "description": "rwbookclub.com — static site for the R/W Book Club, generated from Oliver's SQLite-backed corpus.",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "eleventy",
    "test": "node --test test/*.test.js",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
